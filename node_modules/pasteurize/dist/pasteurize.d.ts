/// <reference types="node" />
export declare class Pasteurize {
    private keyLength;
    private saltLength;
    private iterations;
    private digest;
    constructor(keyLength: number, saltLength: number, iterations: number, digest: string);
    hashPassword(password: string, cb?: (err: Error, hash?: string) => void): Promise<string>;
    hashPasswordSync(password: string): string;
    verifyPassword(password: string, hashedPassword: string, cb?: (err: Error, matches?: boolean) => void): Promise<boolean>;
    verifyPasswordSync(password: string, hashedPassword: string): boolean;
}
